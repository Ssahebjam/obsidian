imagine you want to make object type with your methods you can use a helper function that takes the object type as its parameter and return that object with the methods that you wanted to add . <b>but we dont have to<b/> do that because javascript have a prototype object that can hold all of your methods in one place and you dont have to add them one by one to your objects . objects can be linked to other object with prototype. all of the default methods of javascript are stored in the Object.prototype. not only the objects have a prototype the function are Function.prototype and the Array.prototype is for array.